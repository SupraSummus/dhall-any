--- dhall-lang/standard/function-check.md	2019-10-14 17:04:42.967901045 +0200
+++ build/semantics_raw/function-check.md	2019-10-14 18:37:14.227947724 +0200
@@ -12,8 +12,8 @@
 judgement ⋁:
 
 
-    ─────────────────────  ; Type < Kind < Sort
-    T₀ ⋁ T₁ = max(T₀, T₁)
+;    ─────────────────────  ; Type < Kind < Sort
+;    T₀ ⋁ T₁ = max(T₀, T₁)
 
 
 ... where `T₀` and `T₁` are either `Type`, `Kind`, or `Sort` (though they may

--- dhall-lang/standard/function-check.md	2019-10-14 17:04:42.967901045 +0200
+++ build/semantics_raw/function-check.md	2020-04-05 18:14:10.837624531 +0200
@@ -12,8 +12,8 @@
 judgement ⋁:
 
 
-    ─────────────────────  ; Type < Kind < Sort
-    T₀ ⋁ T₁ = max(T₀, T₁)
+;    ─────────────────────  ; Type < Kind < Sort
+;    T₀ ⋁ T₁ = max(T₀, T₁)
 
 
 ... where `T₀` and `T₁` are either `Type`, `Kind`, or `Sort` (though they may
